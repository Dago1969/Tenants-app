import { Component } from '@angular/core';

/**
 * Pagina di creazione/modifica progetto (scheletro, da completare).
 */
@Component({
  selector: 'app-projects-crud',
  standalone: true,
  template: `<div>Gestione progetto (in sviluppo)</div>`
})
export class ProjectsCrudComponent {}
